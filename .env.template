# Environment Variables Template
# Copy this file to .env and fill in your actual values

# MongoDB Atlas Database
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# Brevo Email Service
BREVO_EMAIL=your-email@example.com
BREVO_SMTP_LOGIN=your-smtp-login@smtp-brevo.com
BREVO_SMTP_PASSWORD=your-smtp-password
ADMIN_EMAIL=admin@example.com

# Stripe Payment Processing
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Admin Authentication
ADMIN_SECRET=your-secure-admin-password

# Application Configuration
PORT=4001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Get your MongoDB URI from:
# 1. Login to MongoDB Atlas (https://cloud.mongodb.com)
# 2. Go to Clusters → Connect → Connect your application
# 3. Select Node.js driver
# 4. Copy the connection string and replace YOUR_USERNAME, YOUR_PASSWORD, etc.

# Get your Brevo credentials from:
# 1. Login to Brevo (https://brevo.com)
# 2. Go to SMTP & API → SMTP
# 3. Generate SMTP credentials
# 4. Use the provided login and password

# Get your Stripe keys from:
# 1. Login to Stripe Dashboard (https://dashboard.stripe.com)
# 2. Go to Developers → API Keys
# 3. Use the Secret Key (starts with sk_)
# 4. For webhooks, go to Developers → Webhooks → Add endpoint
